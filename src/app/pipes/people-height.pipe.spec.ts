import { PeopleHeightPipe } from './people-height.pipe';

describe('PeopleHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
